import React from 'react';
import {View, Text, StyleSheet, FlatList, Image, Dimensions, Animated, TouchableOpacity, Platform} from 'react-native';


const RecommendScreen = () => {
    return (
        <View>
            <Text>
                RecommendScreen
            </Text>
        </View>
    );
};

export default RecommendScreen;